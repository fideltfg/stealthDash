# Optional production overlay: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build dashboard
# The base stack also uses the production image, so ordinary restarts stay in production mode.
services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.prod
