services:
  dashboard-prod:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: stealth-dashboard-app-prod
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
